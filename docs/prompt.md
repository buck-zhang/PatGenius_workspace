# 下記要求を満たせれるコードを生成してください
# 分類コード検索エンジンの要求
- フォルダdata_20250812にある全てのデータを確認して構造を理解してください
- 上記フォルダdata_20250812にある全てのデータは特許分類のデータであり、これらのデータを検索可能なRAG付のデータベースにしてください。
- データベースのみならず、呼び出し可能なfast apiに作ってください。
- 検索の機能要求として以下です。
  - 入力：OR条件AND条件が可能に
    - キーワード
    - テキスト文章
    - 画像
    - IPC
    - CPC
    - FI
  - 出力
    - IPC,CPC,FIのコードとそのコードの解釈
- fast apiのpytonのサンプルコードも作ってください。


# 下記要求を満たせれるコードを生成してください
# google patent検索APIの要求
- google patent：https://patents.google.com/に対して、キーワードとFIの検索を可能なAPIを構築(Webスクレイピングを用いながら)
  - 入力:OR,AND,NOT,NEARなどgoogle patentがサポートしている高度など検索が可能に
  - 出力
    - 検索のヒットした特許の総件数
    - 検索のヒットした全ての特許のCPCのランキング
    - ヒットした全ての特許の特許番号
    - ヒットした全ての特許のPDF

 
# 下記要求を満たせれるコードを生成してください
# 構成要件分割の関数の要求
- gemini利用の情報
  - 下記SAサービスアカウントのjsonを用いて、gemini2.5proモデルを利用
    - /Volumes/T7/patgenius/zhang_opera/ttdc-in-house-dev-3e07247326cb.json
    - google platform project id: ttdc-in-house-dev
    - geminiの利用はvertexAI経由
- 入力
  - PDF/XML形式の特許データ
- 処理内容と手順
  - 本願理解
    - 入力されたPDF/XML形式の特許データを下記の構成分割プロンプトと一緒にgeminiリクエスト
      - 構成分割プロンプト
        - 入力されたPDF/XMLのデータの特許の全てを確認して下記の要求通りに構成要件表を生成
        - 要求
          - 発明全体と完全に一致する先行技術（ドンピシャの文献）が見つかることは稀です。
          - 発明を小さな要素（構成要件）に分解することで、個々の要素や、いくつかの要素の組み合わせを開示する文献を広く探し出すことができます。
          - 特許性を判断する上で、新規性だけでなく**進歩性**（容易に思いつけたか否か）も重要です。
          - 各構成要件が公知であるか、また、それらを組み合わせることが容易であったか（動機付けがあるか）を検討するために、まず発明を要素に分解する必要があります。
          - 発明全体を表すキーワードだけで検索すると、膨大なノイズ（無関係な文献）がヒットしたり、逆に関連文献が漏れたりしがちです。
          - 分割した構成要件ごとにキーワードや特許分類（IPC/FI/CPC）を検討することで、より的確な検索式を作成できます。
          - 分割方法は以下ですが入力特許の内容によって適切な方法を選択して分割
          - クレームの文言に従う
            - クレームの記載を文法的に解釈し、「〜部材と、」「〜手段と、」「〜する工程と、」といった**名詞句**や**動詞句**を単位として抽出します。
            - 「及び」「並びに」「であって」「有し」「備え」などの**接続詞**や**句読点（、。）**は、要素の区切りを示していることが多いです。
          - 機能的・構造的単位で区切る
            - 発明を構成する**部品・部材**（構造的単位）や、それらが果たす**役割・機能**（機能的単位）に着目して分割します。
            - 例：「データを入力する**入力部**（構造）と、入力されたデータを記憶する**記憶部**（構造）と、記憶されたデータに基づいて演算処理する**制御部**（構造・機能）と、・・・」
          - 発明の「課題解決手段」を意識する
            - 発明が解決しようとする課題（従来技術の問題点）と、その課題を解決するための手段（本発明の特徴）を意識します。
            - 特に**特徴部分**（従来技術と異なる部分）は、調査の核となるため、より詳細に分割・分析することが重要です。
          - 「必須の構成」か「任意の構成」かを見極める
            - クレームには、発明の成立に不可欠な「必須の構成要件」と、任意（付加的）な構成要件が含まれる場合があります。
            - まずは**必須の構成要件**（独立請求項に記載の全要件）を漏れなく抽出することが基本です。
          - 細かすぎる分割**: あまりに細かく（例えば単語レベルまで）分割しすぎると、各要素が一般的すぎて（例：「ネジ」「板」など）、調査ノイズが増えすぎてしまう可能性があります。意味のある機能的・構造的なまとまりを意識することが重要です。
          - 発明の一体性**: 分割はあくまで分析のためであり、最終的にはそれらの要素が**組み合わさって**本願発明の課題を解決し、効果を奏していることを念頭に置く必要があります。
          - 構成要素の番号を振ってください：クレームごとは数字の１、２、３、クレームの中の要素はa,b,c
          - 構成要素ごとにに要素のテキストが必要
          - 構成要素ごとに全文の中、上記構成要素をサポートする記載箇所が必要
          - 構成要素ごとに上記構成要素の簡単説明のテキスト
          - 構成要素ごとに上記構成要素が前後の構成要件の従属関係を簡単な記号で表記
          - 構成要素ごとに上記構成要素が特徴部なのかを0~1の間の数字で評価
          - 出力フォーマット配列とjson：[{"構成要素番号":"1a","構成要素":"＊＊","構成要素のサポート箇所":"**","構成要素の簡単説明":"***","構成要素の従属関係":"|","構成要素の重要度":"0.8"},{"構成要素番号":"2b","構成要素":"＊＊","構成要素のサポート箇所":"**","構成要素の簡単説明":"***","構成要素の従属関係":"|","構成要素の重要度":"0.8"}]
    - geminiからのレスポンスとして特許請求の範囲の構成要件が所定の形式で帰ってくる
  
  - 検索式作成
    - キーワード特定
      - 上記geminiから帰ってきた構成要件をもとにそれぞれの構成要素の検索キーワードを下記の要求で生成
        - 構成要素検索キーワード生成要求
          - 各要素の名称（キーワード）の抽出:分解した各構成要素の「名称」をそのままキーワードとして抜き出します。（例：「モーター」「ネジ」「制御部」）
          - 同義語・類義語への展開:抜き出したキーワードの「同義語」や「類義語」を洗い出します。（例：「ネジ」→「ボルト」「締結部材」「螺子」）
          - 上位概念・下位概念への展開:キーワードの「上位概念」（より広い意味の言葉）と「下位概念」（より狭い意味の言葉）を考えます。（例：「モーター」→（上位）「駆動源」「アクチュエータ」、（下位）「DCモーター」「ステッピングモーター」）
          - 作用・機能・目的の言語化:各構成要素が持つ「役割（作用・機能）」や「目的」を表す言葉もキーワード候補にします。（例：「ネジ」→「固定する」「締結する」「着脱可能」）
          - 業界用語・専門用語の確認:その技術分野特有の「専門用語」や「業界用語」、あるいは「古い表現」がないか確認します。
          - 外国語（特に英語）の確認:日本語キーワードに対応する「英語」のキーワードも確認しておくと、外国特許の調査やIPC分類の理解に役立ちます。    
    - 特許分類コード特定
      - 上記geminiから帰ってきた構成要件をもとにそれぞれの構成要素の特許分類コードについてopensearchの特許分類コード検索 APIを用いて下記の要求で検索して特定
        - 構成要素特許分類コード検索要求
          - 各構成要素ごとに構成要件のテキスト、簡単説明、前後の従属関係でopensearchの特許分類コード検索 APIで特許分類コードを検索：FIを出力、FIがなければIPCで出力
      - 上記geminiから帰ってきた構成要件をもとにそれぞれの構成要素の特許分類コードについて下記要求の予備検索を実施
        - 予備検索要求
          - 上記で特定したキーワード（特に発明の核心を表すもの）を使い、google patents検索APIで検索します。
          - 検索結果から、CPCのランキン結果を取得
          - 上記 CPCの検索結果をopensearchの特許分類コード検索 APIでFIに変換して出力
      -  opensearchの特許分類コード検索 APIの結果の特許分類コードと予備検索の分類コードの結果の交わる特許分類を最終の各構成要素の特許分類に特定
     -  特許検索式作成
         -  上記構成要件を用いて、上記生成した各構成要素の検索キーワード、特定した各構成要素のFIを用いて下記要求（特許検索セオリー）の検索式を作成
            -  一個の構成要素のFI(FIが複数の場合OR条件)を異なる構成要素のキーワードとAND条件の式を作成。
            -  上記式をそれぞれの構成要素で作成し最終的にOR条件で結合
   -  特許検索
      -  上記作った特許検索式を用いてgoogle patents検索APIを用いて検索
      -  ヒット件数が10件から50件の間の場合、上記ヒットした件数の特許のPDFを全て取得
      -  ヒット件数が10件以下の場合、下記検式索範囲拡大要求通りに検索式を再構築して検索を再度実行、ヒット件数が10件以上になるまで繰り返し
         -  検式索範囲拡大要求
            -  各構成要素の検索キーワードの生成範囲拡大
               - 同義語・類義語の追加: 「自動車」だけでなく「車両」「乗り物」「ビークル」なども追加します。（例: 自動車 OR 車両 OR 乗り物）
                - 広義語（上位概念）の採用: 「セダン」でヒットしないなら、上位概念の「自動車」で検索します。
                - 表記の揺れ・略語の考慮: 「AI」と「人工知能」、「コンピュータ」と「コンピューター」など。
                - ワイルドカード・近接演算子の活用:ワイルドカード: コンピュ*（前方一致：コンピュータ、コンピューティング等）で表現の幅を持たせます。
        - FI検索と特定の拡大
          - 上位の分類コードを採用: 例えば、詳細な分類 G06N 3/08（特定のニューラルネットワーク）だけでなく、上位の G06N 3/00（機械学習全般）まで広げます。
          - 関連する分類コードの追加: 主目的の分類だけでなく、関連する応用分野や部品の分類もORで追加します。
      - ヒット件数が50件以上の場合、下記検式索範囲縮小要求通りに検索式を再構築して検索を再度実行、ヒット件数が50件以下になるまで繰り返し
        - 検式索範囲縮小要求
          - 各構成要素の検索キーワードの生成範囲縮小
            - 必須のキーワードを追加（AND）: 「A」だけでは広すぎる場合、「A AND B」のように、必須の構成要素Bを追加します。（例: 自動車 AND 電気）
            - 狭義語（下位概念）の採用: 「自動車」 → 「電気自動車」のように、より具体的な言葉を使います。
            - 不要なキーワードの除外（NOT）: 明らかに不要な概念Cが含まれる文献を除く場合、「A NOT C」とします。（※ただし、必要な文献まで除外してしまうリスクがあるため注意が必要です）
          - FI検索と特定の縮小
            - 下位の分類コードを採用: 上位の分類 G06N 3/00 → より具体的な G06N 3/08 に絞り込みます。
            - 複数の分類コードの掛け合わせ（AND）: Aの分類 AND Bの分類、のように複数の技術的観点を持つ文献に絞ります。特にFターム（日本独自）は多角的な絞り込みに有効です。


JP2014007731Aをgoogle patents検索APIで特許のPDFを取得して構成要件分割の関数で特許のリストを取得
